{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "BranchSetupResult",
  "description": "Output contract for phase-6 branch setup. Kept as a schema even when phase-6 is purely scripted, for future use if an LLM dispatch is added.",
  "type": "object",
  "required": ["branchName", "created", "baseBranch"],
  "properties": {
    "branchName": {
      "type": "string",
      "description": "The full branch name (e.g. feat/my-feature-pv-1mt6-6)"
    },
    "created": {
      "type": "boolean",
      "description": "True if a new branch was created, false if already on the correct branch"
    },
    "baseBranch": {
      "type": "string",
      "description": "The branch this was created from (usually 'main')"
    }
  },
  "additionalProperties": false
}
