<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>{{t 'title'}}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 5px 5px 0 0;
            border-bottom: 3px solid #E2904A;
        }
        .content {
            padding: 30px 20px;
            background-color: #ffffff;
        }
        .highlight {
            background-color: #fff3e7;
            padding: 15px;
            border-radius: 5px;
            margin: 20px 0;
            border-left: 4px solid #E2904A;
        }
        .footer {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 0 0 5px 5px;
            font-size: 12px;
            color: #666;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>{{t 'title'}}</h1>
        </div>

        <div class="content">
            <p>{{t 'greeting'}}</p>

            <p>{{t 'notification'}}</p>

            <div class="highlight">
                <strong>{{t 'event_label'}}</strong> {{eventName}}<br>
                <strong>{{t 'category_label'}}</strong> {{reportCategory}}<br>
                <strong>{{t 'report_count_label'}}</strong> {{reportCount}}
            </div>

            <p>{{t 'action_message'}}</p>

            <p>{{t 'signature'}}<br>
            {{t 'team_name'}}</p>
        </div>

        <div class="footer">
            {{t 'footer_text'}}
        </div>
    </div>
</body>
</html>
