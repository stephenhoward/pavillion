<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>{{t 'title'}}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .button {
            display: inline-block;
            padding: 10px 20px;
            background-color: #4A90E2;
            color: white;
            text-decoration: none;
            border-radius: 4px;
            margin: 20px 0;
        }
        .footer {
            margin-top: 30px;
            font-size: 12px;
            color: #777;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>{{t 'title'}}</h1>
        <p>{{t 'greeting'}}</p>
        <p>{{t 'invitation_message'}}</p>

        <p><a href="{{invitationUrl}}?code={{inviteCode}}" class="button">{{t 'button'}}</a></p>

        <p>{{t 'copy_link_text'}} <br>
        {{invitationUrl}}?code={{inviteCode}}</p>

        <p>{{t 'expiry_notice'}}</p>

        <p>{{t 'signature'}}<br>
        {{t 'team_name'}}</p>

        <div class="footer">
            <p>{{t 'footer_text'}}</p>
        </div>
    </div>
</body>
</html>