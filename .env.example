# Pavillion Environment Variables
#
# Copy this file to .env and configure your secrets.
# Docker Compose will automatically load variables from .env
#
# RECOMMENDED: Run ./bin/setup.sh to automatically generate secure secrets!
#
# IMPORTANT: Never commit your .env file to version control!
#
# For full documentation, see docs/configuration.md
#

# =============================================================================
# Security Secrets (Required)
# =============================================================================
# These secrets should be unique, random values for each deployment.
# Run ./bin/setup.sh to generate them automatically, or use:
#   openssl rand -base64 32
#
# SECURITY MODEL:
# - Secrets are stored in .env with 600 permissions (owner read/write only)
# - Docker Compose reads from .env and passes to containers
# - For enhanced security, use Docker secrets (_FILE pattern) - see docs
# - Always backup secrets to a password manager (Bitwarden, 1Password, etc.)

# JWT secret for API authentication tokens (required, no default)
# Used to sign and verify JWT tokens for the REST API
JWT_SECRET=your_jwt_secret_here

# Session secret for cookie signing (required, no default)
# Used to sign session cookies for web authentication
SESSION_SECRET=your_session_secret_here

# Database password (required, no default)
DB_PASSWORD=your_secure_database_password_here

# =============================================================================
# Database Configuration (Optional)
# =============================================================================

# Database name (optional, default: pavillion)
DB_NAME=pavillion

# Database user (optional, default: pavillion)
DB_USER=pavillion

# =============================================================================
# Application Configuration (Optional)
# =============================================================================

# Application port (optional, default: 3000)
# APP_PORT=3000

# =============================================================================
# S3 Storage Configuration (Optional)
# =============================================================================
# Configure these if you want to use S3-compatible storage for media files.
# If not configured, media files will be stored in the pavillion-media volume.
#
# Also requires setting media.storage.driver: 's3' in config/local.yaml

# S3 bucket name
# S3_BUCKET=your-bucket-name

# S3 region
# S3_REGION=us-east-1

# S3 access key (AWS access key ID)
# S3_ACCESS_KEY=AKIAIOSFODNN7EXAMPLE

# S3 secret key (AWS secret access key)
# S3_SECRET_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY

# S3-compatible endpoint (optional, for DigitalOcean Spaces, MinIO, etc.)
# S3_ENDPOINT=https://nyc3.digitaloceanspaces.com

# =============================================================================
# SMTP Configuration (Optional)
# =============================================================================
# Configure these for sending emails (password reset, invitations, etc.)
# Can also be configured in config/local.yaml

# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your_smtp_username
# SMTP_PASSWORD=your_smtp_password

# =============================================================================
# Advanced Configuration (Optional)
# =============================================================================

# Database connection timeout for container startup (seconds, default: 30)
# DB_WAIT_TIMEOUT=30

# Log level (error, warn, info, debug)
# LOG_LEVEL=info
