# Production configuration overrides
# Secrets (jwt.secret, session.secret, moderation.emailHashSecret) MUST be
# provided via environment variables - see custom-environment-variables.yaml

database:
  dialect: postgres
  logging: false
  storage: null   # Explicitly clear SQLite storage from default.yaml

mail:
  transport: smtp
